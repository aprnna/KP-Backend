1. Async Python Development
   The agent understands asyncio, async context managers, and asynchronous HTTP clients.

2. FastAPI Application Architecture
   The agent can modify FastAPI routers, dependencies, and service layers.

3. SQLAlchemy Async ORM
   The agent can implement async session management, bulk inserts, and transaction control.

4. ETL Pipeline Design
   The agent understands Extract → Transform → Load pipelines and can improve batching, idempotency, and reliability.

5. API Integration
   The agent can integrate with external APIs such as Crossref and OpenAlex.

6. Concurrency Control
   The agent can implement concurrency using asyncio.Semaphore and asyncio.gather.

7. Retry and Backoff Logic
   The agent can implement exponential backoff strategies for network and API errors.

8. Structured Logging
   The agent can improve logging for debugging and operational visibility.

9. Data Normalization
   The agent can implement normalization rules for author names and identifiers.

10. Performance Optimization
    The agent can detect inefficient database access patterns and replace them with bulk operations.
